<%= title @habit.name + " Entries" %>

<div class="card-container">
    <%= render @habit.entries %>
</div>

<div class="button-container">
    <%= link_to "Back", habit_path(@habit), class: "primary-button" %>
    <%= link_to "New Entry", new_habit_entry_path(@habit), class: "primary-button" %>
</div>