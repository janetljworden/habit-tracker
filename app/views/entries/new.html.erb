<% title "New Entry" %>

<%= bootstrap_form_with(model: [@habit,@entry]) do |f| %>
    <%= f.datetime_field :date %>
    <%= f.text_field :status %>
    <%= f.text_area :notes %>
    <%= f.primary "Create", class: "primary-button" %>
<% end %>