<% title "My Habits" %>

<div class="card-container">
    <% @habits.each do |habit| %>
        <%= render habit %>
    <% end %>
</div>

<div class="button-container">
    <%= link_to "New Habit", new_habit_path, class: "primary-button" %>
</div>